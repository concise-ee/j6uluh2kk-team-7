apiVersion: v1
kind: Service
metadata:
  name: TEAM-service
  namespace: TEAM
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 3080
      protocol: TCP
      name: http
  # pods selector for routing traffic
  selector:
    app: TEAM-app
